# The web app talks to the API at the same origin by default (Vite dev-proxies
# /api → localhost:3001). Set this only if the API is hosted elsewhere.
# VITE_API_BASE=https://your-api-host.example.com
